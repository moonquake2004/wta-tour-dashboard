{
  "name": "wta-tour-dashboard",
  "version": "2.0.0",
  "private": true,
  "description": "An independent data dashboard for the Hologic WTA Tour, built with the Python standard library from the official WTA public data feed.",
  "keywords": ["wta", "tennis", "dashboard", "rankings", "sports-data", "static-site", "python"],
  "license": "MIT",
  "engines": { "python": ">=3.11" },
  "scripts": {
    "build": "python3 pyscripts/build.py",
    "refresh": "python3 pyscripts/refresh_rankings.py",
    "data": "python3 pyscripts/generate_data.py",
    "site": "python3 pyscripts/build_site.py",
    "serve": "python3 pyscripts/serve.py",
    "verify": "python3 pyscripts/verify.py && python3 pyscripts/verify_dashboard.py",
    "publish": "python3 pyscripts/generate_data.py && python3 pyscripts/build_site.py && python3 pyscripts/verify_dashboard.py"
  }
}
